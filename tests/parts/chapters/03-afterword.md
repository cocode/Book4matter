# Afterword {.section}

This afterword uses `# ... {.section}`. It should render like an unnumbered
chapter -- an all-caps title with no number, upright justified body text -- and
appear at the *top level* of the contents, not nested beneath Part Two.

If chapters carry all-caps titles, "Afterword" matches them. Write to the author
at <hello-team@example.com>, an address that must never break at its hyphen.
